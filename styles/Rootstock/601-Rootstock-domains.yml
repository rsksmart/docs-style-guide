extends: substitution
message: "Use '%s' domain in '%s'"
ignorecase: true
level: error
scope: raw
action:
  name: replace
swap:
  - '^(?!.*(https://public-node\.testnet\.rsk\.co|https://public-node\.rsk\.co)).*rsk\.co.*': rootstock.io
  - '.*rifos\.org.*': rif.technology